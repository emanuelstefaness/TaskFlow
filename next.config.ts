import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Remove o painel / atalho de Dev Tools do Next no canto da tela em desenvolvimento. */
  devIndicators: false,
};

export default nextConfig;
